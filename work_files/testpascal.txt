

#     This program displays the first few iterations 
#     of the fibonacci sequence


start
   
    var a = 0
    var b = 1
    bin t = a < 3
    
    # should array start at 0 or 1 ?

    var[13] var_array = { 1, 2, 3, 4, 5, 6, 43, 4641 }
    disp( var[0] )
    >> 1


    bin[10] bin_array = { 0, 1, 0, 1, 0, 1 }
    disp( bin[2] )
    >> False


    disp( t )

    for i = 0 to 10 do
        b = b + a           # Avoid using a temporary variable         
        a = b - a
        disp( b )
    end

stop


if( a == 0 ) then
    disp( "OK" )
end


procedure square( var a )
    
    return a * a

end

function square( var a )
    
    return a * a

end


summary :

    - no semi-colon
    - being statement always implied
        -> end statement always required
    - var and bin keyword to declare variables ( int and bool )
    - [] operator to declare array
    - func to declare functions ?
